# Cross-Model Analytics

Scripts here compare the single-country and two-country generated result sets.

- `compare_single_two_country.jl`: reads full-grid generated CSVs from
  `results/single_country/generated` and `results/two_country/generated`, then
  writes comparison and diagnostic summaries under
  `results/cross_model/analytics`.
