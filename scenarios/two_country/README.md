# Two-Country Scenarios

Scenario scripts for the two-country extension live here.

- `run_fiscal_parameter_regions.jl`: runs the two-country version of the
  fiscal parameter-region grid and writes generated CSVs under
  `results/two_country/generated`.
